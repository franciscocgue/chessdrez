const logic = {
    possibleMoves: (piece: string, row: number, col: number) => { },
};

// possible moves
logic.possibleMoves = (piece: string, row: number, col: number) => {

    let moves: string[] = [];

    if (piece === 'torre') {

    }

    return moves;
}




export default logic;