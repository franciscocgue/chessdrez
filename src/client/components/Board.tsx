import React from 'react';
import Cell from './Cell';

const Board = () => {
    return (
        <div>
            <Cell piece={null} posH={1} posV={1} />
        </div>
    )
};

export default Board;